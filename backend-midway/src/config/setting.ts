/** 项目的自定义配置 */

// 是否开启  操作日志记录
export const isUseOperLog: boolean = true;

// 是否开启  登录日志记录
export const isUseLoginLog: boolean = true;

// 是否开启  权限校验 升级到redis缓存
export const isUseAuth: boolean = true;

// 是否开启  验证码校验  升级到redis缓存
// export const isCaptchaEnabled: boolean = false;

// 是否开启 加密 升级到redis缓存
export const isOpenCrypt: boolean = false;
